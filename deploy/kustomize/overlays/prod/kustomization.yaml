apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: forge


resources:
- ingress.yaml
- ../../base
  # SealedSecret for DB DSN, Anthropic key, OIDC secret
  # Generate with: kubeseal --format yaml < secret.yaml > sealed-secret.yaml
  # - sealed-secret.yaml   # uncomment after sealing

images:
- name: forge-backend
  newName: git.shadyknollcave.io/micro/forge-backend
  newTag: latest
